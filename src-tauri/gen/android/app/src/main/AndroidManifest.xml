<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.cloudv2ray"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.plugin.cloudv2ray.PluginReceiver" android:exported="false">
            <intent-filter>
                <action android:name="plugin.cloudv2ray" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name="com.plugin.cloudv2ray.V2RayVpnService"
            android:enabled="true"
            android:exported="false"
            android:label="@string/main_activity_title"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:process=":RunV2RayCoreDaemon">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <!--            <meta-data-->
            <!--                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"-->
            <!--                android:value="true" />-->
            <!--            <property-->
            <!--                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"-->
            <!--                android:value="vpn" />-->
        </service>
    </application>
</manifest>